# Registry catalogs

Generated projections from trusted registries. Authority remains the runtime
registries; regenerate with:

```bash
uv run python scripts/generate_registry_catalogs.py
```

Contents: `workflows.json`, `capabilities.json`, `connectors.json`, and combined
`catalog.json`.
