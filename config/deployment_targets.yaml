# Hermetic simulated deployment targets. Production is intentionally unsupported.
# Legacy target id `staging-local` resolves to `simulated-local` via alias.
targets:
  - target_id: simulated-local
    environment: staging
    adapter: in_process
    enabled: true
    health_checks:
      - rollout
      - error_rate
